spring:
  application:
    name: LMS
  datasource:
    url: jdbc:postgresql://localhost:5442/lms_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml

server:
  port: 8080